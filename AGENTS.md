# 仓库协作约束

本文件对整个仓库生效。

## 修改前必读

开始工作前必须完整阅读：

1. `standards/ENGINEERING_STANDARD.md`
2. `standards/project-policy.yaml`
3. `standards/plugin-manifest.schema.yaml`
4. 被修改插件的 `plugin.yaml`，以及实际存在的插件接入文档

可机器判断的规则以 YAML 和 Schema 为准；语义和操作边界以工程硬标准为准。局部约定可以更严格，但不能削弱仓库级要求。

## 执行要求

- 只提交达到上线质量的实现、契约、测试和打包配置。
- 插件按需声明实际支持的目标，禁止空目录、占位适配器和未来平台预埋。
- 每次只完成请求所需的最小、完整、干净变更。
- 禁止为了历史实现或本地测试数据加入兼容路径、别名、双轨逻辑或降级读取。
- 无效和未知输入必须明确失败，禁止字段猜测、别名探测、静默归一化、吞错和伪造默认值。
- 命名必须表达当前职责，禁止使用阶段、优先级、时间顺序或替换历史作为内部名称。
- 按职责和变化原因保持合理模块化：既禁止失控的巨型文件，也禁止为了缩短文件而机械切碎。
- 插件实现只存在于 `plugins/<plugin-id>/`，测试工程不得成为实现来源。
- 打包变更必须检查最终产物，不能只验证源码工程能够构建。

## 本地数据库破坏性操作

本仓库拥有的本地开发和验证数据可以放弃，不能为了保护这些数据编写兼容代码。

但任何删库、清库、截断、重建、重置或破坏性迁移，都必须在执行前重新取得用户的明确二次确认。确认前必须先只读查明并告知：

- 精确数据库、文件、服务或容器；
- 环境及其属于本地非生产数据的证据；
- 将执行的具体操作；
- 数据损失范围；
- 恢复方式，或明确说明不可恢复。

制定本规则的请求不等于未来操作授权，普通实现请求也不能视为确认。该规则永远不能用于推断可以处理远端、预发布或生产数据库。
